//! Bouncer binary entry point.
//!
//! Scaffolding only: real startup (single-instance guard, spawning the hook and
//! UI threads, wiring the `Command`/`Report` channels) arrives with the backend
//! (#7) and the tray/UI (#9, #10).

fn main() {
    eprintln!("bouncer: scaffolding build — not yet functional");
}
